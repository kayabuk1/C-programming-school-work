#include<stdio.h>

int main(int argc, char* argv[]) {
	int i;
	printf("argcの値は%d\n", argc);
	for (i = 0;i < argc;i++)
		printf("argv[%d]の文字列は,%s\n", i, argv[i]);
	return 0;
}
/*
普通に実行した出力
argcの値は1
argv[0]の文字列は,

コマンドラインで、ファイル名を打ち込み1008_復習模写7_8から.c、で実行すると
argcの値は2
argv[0]の文字列は,D:\workspace\iot05SourceReposC-programming-school-work\x64\Debug\0908アドレスとポインタ.exe
argv[1]の文字列は,1008_復習模写7_8から.c
この様な出力になる。

コマンドライン引数の目的についての質問、とても良い点です。main関数が (int argc, char *argv[]) という引数を受け取れることの「意義」や「目的」ですね。

## コマンドライン引数の目的：「ツール」を作ること
コマンドライン引数の最も大きな目的は、プログラムの動作を、起動するたびに外部から柔軟に変更できるようにすることです。

これにより、プログラムが単なる「一度きりの作品」から、何度でも使える便利な「ツール」に進化します。

▼ 例え：決まった料理しか作れないレストラン vs 注文できるレストラン
引数がないプログラム (int main(void)):
「本日のランチはカレーです」と、決まった料理しか作れないレストランのようなものです。もしパスタが食べたければ、シェフ（プログラマー）がレシピ（ソースコード）を書き換えるしかありません。

引数があるプログラム (int main(int argc, char *argv[])):
お客さん（ユーザー）が「注文」できるレストランです。
my_restaurant pasta cheese
という注文（コマンドライン実行）があれば、シェフ（プログラム）は注文票（argv）を見て、「pastaにcheeseをトッピングして作ればいいんだな」と、その場で動作を変えることができます。

## 具体的には、どんな場面で役立つのか？
1. ファイル名を指定する
あなたがC言語をコンパイルするときに使った gcc コマンドが、まさにこれです。
gcc -o my_app main.c

このとき、gccというプログラムは、コマンドライン引数として渡された -o my_app main.c を見て、

「-oが指定されたから、出力ファイル名はmy_appにしよう」

「main.cというファイルが指定されたから、これをコンパイルしよう」
と判断しています。

2. プログラムの動作を切り替える（オプション）
ファイルの一覧を表示する ls コマンドを考えてみましょう。

ls → ファイル名だけ表示

ls -l → 詳細情報も表示

-lというコマンドライン引数（オプション）を受け取ったかどうかで、lsプログラムは出力形式を切り替えています。

3. 自動化のため
コマンドライン引数を使えば、ユーザーが毎回キーボードで入力する必要がなくなります。
例えば、「100個のファイルを自動で処理する」といったスクリプトを書くとき、ループの中でプログラムを100回、毎回違うファイル名を引数として渡しながら実行する、といったことが可能になります。

まとめ:
コマンドライン引数は、プログラムを再利用可能で自動化しやすい「便利なツール」にするための、非常に重要な仕組みなのです。












ツール




*/
